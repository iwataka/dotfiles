#!/usr/bin/env bash

set -e

if [[ ! -d $HOME/.nvm ]]
then
    curl -o- https://raw.githubusercontent.com/creationix/nvm/v0.26.1/install.sh | bash
else
    # This script must be overridden by original nvm
    echo "Don't use this"
    exit 1
fi
